const Button = () => {
  return (
    <button className="bg-yellow max-sm:py-2 max-sm:px-4 px-6 py-2 rounded-full max-sm:text-sm  text-lg font-kanit text-gray-slate">
      Play now
    </button>
  );
};

export default Button;
